# About

This is inspired by spot-dl, but that cli was always breaking for me. Either the ytdl lib it depended on, 429s errors, or python versions etc..

So I decided to make a CLI with interactive UI to download music using pure typescript and few npm libs.
